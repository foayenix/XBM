# Floors are the versions whose APIs this code actually uses - not the
# oldest release that happens to import. youtube-transcript-api in
# particular moved from a static get_transcript() to the instance
# .fetch() used in backend/enrich.py at 1.0, so an older resolution
# would fail at runtime rather than at install time.
fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27
python-dotenv>=1.0
anthropic>=0.40
youtube-transcript-api>=1.0
readability-lxml>=0.8
# Plain lxml: backend/enrich.py imports lxml.html directly. The
# html-clean extra is readability-lxml's own dependency and arrives with
# it. Asking for lxml[html_clean] here as well made older pip treat the
# underscore and hyphen spellings of one extra as two, and crash with
# "Candidate is not for this requirement lxml[html-clean,html-clean]".
lxml>=5.2
